apiVersion: v1
kind: Service
metadata:
  name: {{ include "team-management.fullname" . }}-meilisearch
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "team-management.labels" . | nindent 4 }}
    {{- include "team-management.meilisearchSelectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "team-management.meilisearchSelectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 7700
      targetPort: http
      protocol: TCP
